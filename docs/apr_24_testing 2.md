make sure aip happens in the year after enrollment, not the first year

can we make ai enabled on the input file and that would help with baseline prediction

can we add peoples current def to input file